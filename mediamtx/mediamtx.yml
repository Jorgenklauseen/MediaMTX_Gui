rtmpAddress: :1936

pathDefaults:
  runOnReady: curl -L -k -X POST "http://app:5000/api/streams/started?name=$MTX_PATH"
  runOnNotReady: curl -L -k -X POST "http://app:5000/api/streams/stopped?name=$MTX_PATH"